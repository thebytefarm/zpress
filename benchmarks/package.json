{
  "name": "benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench",
    "bench:run": "vitest bench --run"
  },
  "dependencies": {
    "@zpress/config": "workspace:*"
  },
  "devDependencies": {
    "@codspeed/vitest-plugin": "^5.0.0",
    "vitest": "catalog:"
  }
}
